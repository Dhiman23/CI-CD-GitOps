apiVersion: v1
kind: Service
metadata:
  name: go-app
  labels:
    name: go-app
spec:
   ports:
   - port: 80
     targetport: 8080
     protocol: TCP
   selector:
    app: go-app
   type: nodeport


